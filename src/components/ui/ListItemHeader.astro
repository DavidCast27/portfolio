---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props as Props;
---

<header
  class={cn(
    "flex flex-col gap-2 items-start sm:flex-row sm:items-center sm:gap-4 mb-4",
    className
  )}
>
  <slot />
</header>
