---
import { PROJECTS } from "../../data/projects.data.astro";
import ProjectItem from "./ProjectItem.astro";
const { currentLocale } = Astro;
---

{
  PROJECTS[currentLocale as "es" | "en"].map((project) => (
    <ProjectItem {...project} />
  ))
}
